{
  "name": "sendeliver",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd front && npm install && cd ../back && npm install",
    "dev": "concurrently \"cd front && npm run dev\" \"cd back && npm run dev\"",
    "start": "npm run front:build && cd back && npm start",
    "start:prod": "npm run front:build && NODE_ENV=production cd back && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "webpack-dev-server": "^5.1.0"
  }
}
