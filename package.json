{
  "name": "sendeliver-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "node back/src/scripts/clean.js",
    "install": "cd back && npm ci && cd ../front && npm ci",
    "build": "cd back && npm ci && npm run build && cd ../front && npm ci && npm run build",
    "start": "cd back && npm start & cd front && npm run preview",
    "dev": "cd back && npm run dev & cd ../front && npm run dev",
    "reset": "npm run clean && npm run install && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}