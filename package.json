{
  "name": "sendeliver-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd front && npm install && cd ../back && npm install",
    "dev": "concurrently \"cd front && npm run dev\" \"cd back && npm run dev\"",
    "start": "npm run build && concurrently \"cd back && npm start\"",
    "build": "cd front && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "webpack-dev-server": "^5.1.0"
  }
}
