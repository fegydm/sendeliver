{
  "name": "sendeliver-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "node back/src/scripts/clean.js",
    "install": "npm install --prefix front && npm install --prefix back",
    "build": "npm run build --prefix front && npm run build --prefix back",
    "reset": "npm run clean && npm install && npm run build",
    "dev": "npm run dev:front & npm run dev:back",
    "dev:front": "npm run dev --prefix front",
    "dev:back": "npm run dev --prefix back"
  },
  "dependencies": {}
}
