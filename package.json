{
  "name": "sendeliver-root",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "node back/src/scripts/clean.js",
    "install": "cd back && npm install && cd ../front && npm install",
    "build": "cd back && npm run build && cd ../front && npm run build",
    "start": "cd back && npm start & cd front && npm run preview",
    "dev": "node back/src/scripts/dev.js",
    "reset": "npm run clean && npm run install && npm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^22.13.9"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  }
}
