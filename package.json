{
  "name": "sendeliver-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd front && npm install && cd ../back && npm install",
    "dev": "concurrently \"cd front && npm run dev\" \"cd back && npm run dev\"",
    "start": "cd back && npm start",
    "build": "cd front && npm install @tailwindcss/forms && npm run build",
    "clean": "rm -rf front/build back/dist node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}